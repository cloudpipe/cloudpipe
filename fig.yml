---
cloudpipe:
  build: .
  links:
  - mongo
  environment:
    PIPE_ADMINNAME: admin
    PIPE_ADMINKEY: 12345
    PIPE_LOGLEVEL: debug
  ports:
  - "8000:8000"
  volumes:
  - "/var/run/docker.sock:/var/run/docker.sock"
mongo:
  image: mongo:2.8
